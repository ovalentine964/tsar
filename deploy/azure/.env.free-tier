# ============================================================
# TSAR — Azure Free Tier Environment Configuration
# ============================================================
# Paper trading only. No Redis, no Ollama, no monitoring.
# Copy to project root: cp deploy/azure/.env.free-tier .env
# ============================================================


# ── Exchange (Binance Testnet) ───────────────────────────────
# Get testnet keys: https://testnet.binance.vision/
EXCHANGE_API_KEY=                       # REQUIRED
EXCHANGE_SECRET=                        # REQUIRED
EXCHANGE_SANDBOX=true                   # Always true for free tier


# ── NVIDIA AI (Required) ────────────────────────────────────
# Free key: https://build.nvidia.com → Get API Key
NVIDIA_API_KEY=                         # REQUIRED


# ── DeepSeek (Optional — fallback LLM) ──────────────────────
# https://platform.deepseek.com
DEEPSEEK_API_KEY=                       # Optional


# ── TSAR API Security ───────────────────────────────────────
# Generate: python3 -c "import secrets; print(secrets.token_urlsafe(48))"
TSAR_API_KEY=                           # REQUIRED
TSAR_API_PORT=8000
TSAR_TRADING_MODE=paper                 # paper only on free tier
TSAR_ENVIRONMENT=production


# ── CORS ─────────────────────────────────────────────────────
# Set to your Azure container URL after deployment, e.g.:
# https://tsar-app.eastus.azurecontainer.io
TSAR_CORS_ORIGINS=


# ── Database (SQLite — no Redis needed) ─────────────────────
TSAR_DATABASE_BACKEND=sqlite
TSAR_DATABASE_PATH=/app/data/tsar.db
TSAR_REDIS_ENABLED=false


# ── LLM (Cloud only — no Ollama) ────────────────────────────
TSAR_OLLAMA_ENABLED=false


# ── Telegram Alerts (Optional) ──────────────────────────────
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=


# ── Azure Deployment ────────────────────────────────────────
AZURE_RESOURCE_GROUP=tsar-free-rg
AZURE_LOCATION=eastus
AZURE_DNS_LABEL=tsar-app
TSAR_IMAGE_TAG=latest
