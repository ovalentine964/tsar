name: tsar_mobile
description: TSAR Trading Super Agent - Mobile App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  http: ^1.2.0
  intl: ^0.19.0
  fl_chart: ^0.66.0
  flutter_secure_storage: ^9.0.0
  local_auth: ^2.1.8
  shared_preferences: ^2.2.2
  web_socket_channel: ^2.4.0
  cupertino_icons: ^1.0.6
  google_fonts: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
